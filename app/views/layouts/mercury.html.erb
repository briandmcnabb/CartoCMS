
<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, maximum-scale=1.0, initial-scale=1.0">
    <%= csrf_meta_tags %>
    <title>Mercury Editor</title>
    <%= stylesheet_link_tag    'mercury' %>
    <%= javascript_include_tag '//ajax.googleapis.com/ajax/libs/jquery/1.7.1/jquery.min.js', 'mercury' %>
  </head>
  <body>
    <script type="text/javascript">
      // Set to the url that you want to save any given page to, leave null for default handling.
      var saveUrl = "<%= mercury_save_url %>";

      // Instantiate the PageEditor
      new Mercury.PageEditor(saveUrl, {
        saveStyle:  "form",
        saveMethod: "PUT",
        visible:    true
      });
    </script>
  </body>
</html>